# Database 配置
database:
  host: localhost
  port: 5432
  name: ddd_scaffold
  user: shenfay
  password: "postgres"
  ssl_mode: disable
  max_open_conns: 25
  max_idle_conns: 5
  conn_max_lifetime: 5m
