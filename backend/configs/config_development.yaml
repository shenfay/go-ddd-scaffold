server:
  port: "8080"
  mode: "debug"
  read_timeout: "30s"
  write_timeout: "30s"

database:
  name: "go_ddd_scaffold"
  max_idle_conns: 5
  max_open_conns: 20

logging:
  level: "debug"
  format: "console"