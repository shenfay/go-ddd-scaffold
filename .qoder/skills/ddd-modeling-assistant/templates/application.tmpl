// Package service contains the application service definitions for {{.ModuleName}}
package service

import (
	"context"
	"fmt"
	
	"{{.ProjectModule}}/internal/domain/{{.ModuleName}}/aggregate"
	"{{.ProjectModule}}/internal/domain/{{.ModuleName}}/repository"
)

// {{.ApplicationServiceName}} provides application layer services for {{.ModuleName}}
type {{.ApplicationServiceName}} struct {
	repository repository.{{.RepositoryInterface}}
	// Other dependencies
}

// New{{.ApplicationServiceName}} creates a new {{.ApplicationServiceName}}
func New{{.ApplicationServiceName}}(
	repo repository.{{.RepositoryInterface}},
) *{{.ApplicationServiceName}} {
	return &{{.ApplicationServiceName}}{
		repository: repo,
	}
}

{{.ApplicationServiceMethods}}

// String implements Stringer interface
func (s *{{.ApplicationServiceName}}) String() string {
	return "{{.ApplicationServiceName}}{}"
}