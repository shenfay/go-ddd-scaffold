// Package dao contains the DAO interface definitions for {{.ModuleName}}
package dao

import (
	"context"
	
	"{{.ProjectModule}}/internal/infrastructure/persistence/gorm/{{.ModuleName}}/model"
)

// {{.DaoInterfaceName}} defines the DAO interface for {{.ModuleName}}
type {{.DaoInterfaceName}} interface {
	// CRUD operations
	Create(ctx context.Context, {{.EntityParam}} *model.{{.ModelName}}) error
	GetByID(ctx context.Context, id string) (*model.{{.ModelName}}, error)
	Update(ctx context.Context, {{.EntityParam}} *model.{{.ModelName}}) error
	Delete(ctx context.Context, id string) error
	
	// Query operations
	List(ctx context.Context, offset, limit int) ([]*model.{{.ModelName}}, error)
	Count(ctx context.Context) (int64, error)
	
	{{.CustomQueries}}
}